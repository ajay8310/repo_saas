; Prepend the Node.js install directory to PATH when running lifecycle scripts.
; Required on this machine because node is not on the system PATH, which makes
; package postinstall scripts (e.g. esbuild's `node install.js`) fail.
scripts-prepend-node-path=true

; Reduce noise and avoid interactive funding/audit prompts during install.
fund=false
audit=false
